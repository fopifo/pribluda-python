# PROJECT_NOTES.md — рабочие заметки для помощника

## ПРАВИЛА ВЗАИМОДЕЙСТВИЯ (читать в первую очередь!)
- Пользователь НЕ выполняет команды с внешними путями (Copy-Item из
  «рабочей папки» и т.п.) и НЕ дописывает файлы руками.
- Помощник даёт файлы ТОЛЬКО ЦЕЛИКОМ — пользователь сохраняет поверх.
- Никаких плейсхолдеров «C:\путь\к\...» в командах: только команды,
  которые можно выполнить как есть из корня проекта.

## Состояние детектора (2026-08-17)
- detectors/interval_robot.py: first-fit (откат best-fit) + фикс
  скорости (active[side] отсортирован по last_ts) + ценовые метрики
  (first_price/last_price/price_shift/same_price_ratio/sum_qty —
  справочно, на матчинг не влияют).
- config.py: fast_strict min_repeats=3, twap_strict min_repeats=4,
  правило «ручное переопределение может только ужесточать» (max).
- tests: 10/10.
- ticker_settings.json: версия с ручными порогами (см. файл).

## A/B baseline (старый журнал, 4 дня 10–13.08)
- всего 128 242 сигнала; дубли 46 687; twap-3повторных 56 593;
  20-повторных 2 124.

## Следующие шаги
- Прогнать A/B (run_all_dates + week_signals_review), сравнить.
- Решить про пресеты 4/5 по цифрам.
- Классификатор типов роботов: двигает цену / долбит стену /
  кешбек / спредовый / пачки (один участник) / раунд-трип (анти-скам).
- entry_backtest: «пошёл» = цена ушла в сторону робота на K тиков
  за N секунд после 3-го удара (дефолт N=60, K=2).
- Скальпер заходит с роботом на ~3-м ударе; ложный робот = убыток.