# PROJECT_NOTES.md — рабочие заметки

## Взаимодействие с владельцем (читать в первую очередь)
- Владелец не правит код руками — файлы ТОЛЬКО целиком.
- Правила владельца (2026-08-20): строго по архитектуре (не плодить в корень),
файлы целиком, тщательная проверка перед отправкой, коммиты на русском.
- Команды — как есть из корня проекта.

## Статус 2026-08-22 (утро, после боевого прогона 21.08)
- lua export_trades v3.10: лента сделок, сторона tick-rule (flags в этом
Quik мёртвы), мс из datetime.msec. lua limits_sweep v5: планки, tonumber-фикс
(param_type строкой), ломтики 2/2c. Оба — ОБЯЗАТЕЛЬНЫ к запуску в Quik.
- backend v3: мс из CSV (без перезаписи arrival-time), batch_flash
(>=4 подтверждений за 15с).
- Детектор v5: история пишется в момент подтверждения; stable_qty для
длинных (SMLT/PLZL); адаптивный допуск (12% только для <10s, иначе 5%).
- Вкладка "Статистика" v3: наш поток + эталон, колонка ИСТОЧНИК;
баг путей (4 .parent) исправлен и в stats_tab, и в limits_tab.
- Эталон копится: tools/import_competitor_csv.py (база CSV + supplement).
- make_dump v5: jsonl статистики идут в дамп без ограничения размера.
- Тесты 10/10. Ветка Qwen_coder, remote github.com/fopifo/pribluda-python.
- Владелец днём торгует и присылает скриншоты (наш скринер + конкурент,
часы на обоих) — материал для сверки и докрутки детектора.

## Проверенные особенности Quik/ISS (зонды + боевой прогон)
- param_type у getParamEx — СТРОКА ("1") → только tonumber (limits_sweep v5).
- flags/operation обезличенных сделок не работают → сторона tick-rule.
- Массовый getParamEx (506/цикл) = nil → ломтики; в отдельном скрипте работает.
- ISS: котировки есть, PRICEMAX/PRICEMIN по акциям НЕТ → планки из Quik.
- CLOSE до первой сделки = 0 → PREVWAPRICE; TRADINGSTATUS type=4.
- OPENPERIOD не отдаётся → аукционы по расписанию (+ "АУКЦИОН (распис.)").
- Таблица "Текущие торги" открыта со всеми TQBR и параметрами — иначе
getParamEx пустой.
- Из gui/tabs/<вкладка>/ до корня — 4 .parent (повторяющийся баг путей;
в новых вкладках проверять сразу).

## Наблюдения боевого прогона (для докрутки детектора)
- Наш поток шумнее конкурента: tick-rule сторона даёт ложные серии на
ликвидных тикерах; конкурент чище (у него настоящая сторона).
- Волны 11:28 (12-16s, qty X..X+1) — главный целевой паттерн; ловим их
допуском 5% при мс-точности.
- robots_history.jsonl растёт быстро (сотни КБ за день) — это наш шумный
поток; не пугаться размера, это материал, а не истина.

## Следующие шаги (после дневной сверки с конкурентом)
1. Анализ расхождений по скриншотам (кто какие серии видит, с какого удара).
2. Докрутка детектора до уровня конкурента через replay/A/B;
возможно приближение вида главного окна к виду конкурента.
3. Трёхногие связки (symbol_c), свечи из Quik (CreateDataSource),
индикативная цена аукциона из стакана.
4. Telegram под absolute_rub и роботов (legacy-бот от старого пути).