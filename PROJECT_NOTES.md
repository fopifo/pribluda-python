"""PROJECT_NOTES.md — рабочие заметки
Взаимодействие с владельцем (читать в первую очередь)
Владелец не правит код руками — файлы ТОЛЬКО целиком.
Правила владельца (2026-08-20): строго по архитектуре (не плодить в корень),
файлы целиком, тщательная проверка перед отправкой, коммиты на русском.
Команды — как есть из корня проекта.
ВАЖНО (2026-08-21): документы и make_dump переписывать ТОЛЬКО как строгие
надмножества текущего диска; владелец сверяет длину/содержимое. Никаких
переписываний по памяти.
ОБЯЗАТЕЛЬНО (2026-08-23): Контекст диалога. Перед каждым ответом
перечитывать весь диалог. Помнить решения, не противоречить, не угадывать.
ОБЯЗАТЕЛЬНО (2026-08-25): важные/ценные файлы (PROJECT_NOTES.md,
core/config.py, ticker_settings.json, research/ab_compare.py и др.) —
давать ТОЛЬКО целиком; ПЕРЕД переписыванием просить у владельца текущее
содержимое файла, чтобы не выдумывать. Владелец заменяет файл целиком
присланным текстом.
ОБЯЗАТЕЛЬНО (2026-08-26): ПРОГРЕСС-БАР. Любой скрипт, выполняющийся
больше 5 секунд, ОБЯЗАН иметь визуальный прогресс-бар с процентами,
текущей позицией, скоростью обработки и оценкой времени до завершения
(ETA — оценочное время до завершения). Без прогресс-бара скрипт не считается готовым к сдаче.
Владелец не должен гадать, выполняется скрипт или завис. Формат:
`[██████░░░░] 60% | 2.5M/4.2M | 125K/с | ETA 14с`.

Правила документации (2026-09-04)
======================================================================
Все аббревиатуры и специальные термины при каждом упоминании в каждом
разделе давать с расшифровкой на русском языке в скобках. Примеры:
- TP (истинно-положительные) — совпадение нашего сигнала с эталоном
- FP (ложно-положительные) — наш сигнал, которого нет в эталоне (шум)
- FN (ложно-отрицательные) — сигнал в эталоне, который мы пропустили
- ETA (оценочное время до завершения) — сколько осталось до конца операции
- LEN (длина серии) — количество повторов в серии робота
- A/B (сравнение двух вариантов) — эксперимент с двумя конфигурациями

В таблицах и повторяющихся метриках (например, "TP=49, FN=681, FP=36254")
расшифровки НЕ требуются — они должны быть даны при упоминании в разделе.

Статус 2026-08-21 (вечер)
lua export_trades v3.10: лента сделок, сторона tick-rule, время с мс.
lua limits_sweep v5: планки, tonumber-фикс (param_type строкой),
ломтики 2/2 c. Запускать ВТОРЫМ скриптом в Quik.
backend v3: мс из CSV (без перезаписи arrival-time), batch_flash
(>=4 тикеров подтвердились за 15 c — мигание пачки).
interval_robot v5: история пишется в момент подтверждения.
v6 (анти-шум в выдаче): пары (LEN — длина серии — 2) скрыты; длинные интервалы
(>120 c) — только подтверждённые (LEN>=4).
Вкладка Статистика (v3): наш + конкурент, колонка ИСТОЧНИК;
пути data/ — 4 уровня .parent (баг исправлен).
limits_tab v5: limits_ui.json в корне, автоперенос из gui/.
make_dump v5: jsonl статистики идут в дамп без ограничения размера.
Тесты 10/10. Ветка Qwen_coder, remote github.com/fopifo/pribluda-python.
Владелец днём торгует и присылает скриншоты (наш скринер + конкурент,
часы на обоих) — материал для сверки и докрутки детектора.
Проверенные особенности Quik/ISS (зонды probe_params.lua)
Массовый getParamEx (506 тикеров/цикл) возвращает nil при живом
соединении; одиночные вызовы работают → планки собираем ломтиками 2/2 c.
getParamEx отдаёт param_type СТРОКОЙ → только tonumber.
flags/operation обезличенных сделок не работают → сторона tick-rule.
ISS: котировки есть, PRICEMAX/PRICEMIN по акциям НЕТ → планки из Quik.
CLOSE до первой сделки дня = 0 → PREVWAPRICE; TRADINGSTATUS type=4.
OPENPERIOD/AUCTION* в этом Quik не отдаются → аукционы по расписанию.
Таблица "Текущие торги" должна быть открыта со всеми TQBR и параметрами,
иначе getParamEx пустой (поток параметров не принимается).
Наблюдения боевого дня 2026-08-21 (материал для тюнинга)
Наш поток шумнее эталона: длинные серии 100-600 c с LEN 2-3 — ложные
(конкурент их не показывает). Короткие роботы конкурента (10-60 c) мы
часто пропускаем из-за min_qty=50 (у конкурента qty 10-50) и допуска 5%.
Волны конкурента: ~10:30 / 11:03 / 11:28 (пачка 10-20 тикеров,
интервалы 12-18 c, LEN растёт до 138).
Валюта/фьючерсы (CNY_TOM, Si-9, ED-9) конкурент видит — мы по дизайну
только TQBR (SPBFUT не подключён в Quik).
Находки 2026-08-22 (критично)
TP (истинно-положительные)=0 в ab_compare объяснён: корень — tick-rule в QUIK.
- Детектор на сырой ленте конкурента (sngsp.csv, стороны buy/sell
  настоящие) видит робота `buy qty=[64,65] повт=25 int=15.0s`
  (ровно то, что показывает конкурент).
- Детектор на QUIK-ленте (quik_trades.csv, стороны tick-rule) того
  же робота не видит — стороны рвутся.
- Это означает: детектор работает правильно, проблема в источнике
  данных. Задача следующего цикла — найти источник истинных сторон
  (QUIK API с `operation`, или другая лента).
Исправлен eval в load_ref (ab_compare.py). Было
`eval(line, {"__builtins__": {}}, {})` — падало на JSON null (когда
время не распознано), `except Exception: continue` глотал — запись
эталона исчезала. Стало `json.loads(line)`. Эталон вырос 63→142.
Исправлены голые datetime.now() в `_write_history` (detectors/
interval_robot.py) и в часах/main_window.py — теперь
`datetime.now(MSK)`. Убрано скрытое допущение "системные часы = МСК".
Baseline A/B (сравнение двух вариантов, замер 2026-08-22): Эталон=142, TP=0, FN=142, FP=1270.
Без источника истинных сторон TP не поднимется.
Правило PowerShell для сессий
Длинные here-string в PSReadLine падают с `System.ArgumentOutOfRange
Exception: Значение должно быть больше или равно нулю и меньше,
чем размер буфера` — баг PSReadLine, не пользователя.
Файлы создавать через `python -c "..."` одной командой, либо
разбивать длинные here-string на короткие pipe-команды.
При `@"..."@ | Set-Content` строго следить: `"@` — в самом начале
своей строки.
Находки 2026-08-23
Анализ LiveScreener (пример, не конкурент): разобрали структуру.
- `getPrint.lua` — шлёт сырой `allTrade.flags` по каждой сделке
  (UDP 3588). У них есть настоящие флаги QUIK, значит гипотеза
  про `flags & 4` может быть проверена по их логике.
- `getPrint_2.lua` — тик-рул конкурента (сторона как `C > пред.C`
  или `C < пред.C`, синтетические коды 1025/1026).
- Индикаторы LiveScreener: VOLUME (всплески за 1-4 сек), LEVELS
  (пробои уровней + подход заранее), BIDASK (прострел BID→ASK),
  PERCENT (прострел между ценами), MINUTE (движение за время).
- Пружина: спред тикера относительно индекса IMOEXF
  (`tickerBoss=IMOEXF`, порог 0.2%).
Внедрено из LiveScreener (гибрид, interval_robot не заменён):
- `modules/volume_spike.py` — всплески объёма за 1/2/3/4 секунды
  (аналог VOLUME). Тесты 8/8.
- `modules/level_break.py` — пробои уровней + подход заранее на N
  пунктов (аналог LEVELS). Тесты 7/7.
- `modules/arbitrage/spring_monitor.py` — спред тикера к IMOEXF
  (аналог пружины). Тесты 7/7.
- Интеграция в `connectors/quik/backend.py`: SpringMonitor параллельно
  с IntervalRobotDetector, отдельный поток `imoex_updater` (качает
  IMOEXF раз в 5 сек), `spring_rows` в SharedState.
- Создан `spring_settings.json` (пример: SBER/GAZP/LKOH).
- Все 32 теста проходят (10 робот + 8 volume + 7 level + 7 spring).

Зонды определения стороны (2026-08-23, критично)
Проведены три независимых проверки для решения проблемы TP=0:

1. CreateDataSource vs OnAllTrade (зонд probe_datasource_ticks.lua):
   - Подписались на CreateDataSource(class_code, sec_code, INTERVAL_TICK)
     для SBER/GAZP/LKOH параллельно с OnAllTrade
   - Результат: 78.3% расхождений (347 из 443 сделок)
   - Вывод: CreateDataSource агрегирует сделки иначе (вероятно,
     схлопывает подряд идущие сделки по одной цене)
   - Это НЕ "более точный" источник, а другой уровень агрегации
   - Решение: CreateDataSource НЕ подходит для замены OnAllTrade

2. flags%2 vs текущий tick-rule (зонд probe_datasource_ticks.lua):
   - Сравнили сторону по чётности флагов (флаги % 2 == 1 → sell)
     с текущим методом (цена выше/ниже предыдущей)
   - Результат: 97.3% совпадений (только 12 расхождений из 443)
   - Вывод: текущий tick-rule УЖЕ эквивалентен проверке чётности флагов
   - Решение: гипотеза flags%2 не даёт улучшения

3. Сверка с Алором (research/compare_sides.py):
   - Прямая сверка сторон из quik_trades.csv с истинными сторонами
     из Алора за 2026-08-21 (пятница)
   - Результат: 87.6% совпадений (662495 из 756027 сопоставленных)
   - Топ тикеров: SMLT 90.2%, SBER 88.3%, VTBR 89.0%, MAGN 89.0%
   - Вывод: текущий tick-rule работает корректно, источник данных
     НЕ является причиной "рвущихся серий"

Корень TP=0 НЕ в источнике данных (доказано). Проблема в детекторе
interval_robot.py — слишком строгие критерии (jitter, grid_lock,
interval_tolerance, min_qty).

Выбор метода определения стороны (2026-08-23)
На основе независимого подтверждения из обратной связи (другой проект
использует правило `флаги % 2 == 1 → продажа`) и результатов зондов:
- Выбран метод по чётности флагов как основной (v3.20)
- Старый метод (tick-rule) оставлен в 6-м поле CSV для сравнения
  и возможного отката (помечен как deprecated)
- Сырые флаги записаны в 7-м поле (для диагностики)
- Формат: символ;объём;цена;сторона_флаги;время;сторона_tickrule;флаги
- Боевой backend.py безопасен (читает первые 5 полей, лишние игнорирует)

export_trades.lua v3.20 (готов к применению):
- Основной метод: flags % 2 (нечётный = sell, чётный = buy)
- Старый метод (deprecated): tick-rule (цена выше/ниже)
- Оба метода записываются для сравнения
- Следующий шаг: применить в понедельник (обычная сессия), собрать
  свежую ленту, прогнать ab_compare.py, проверить TP

Точечные минимальные объёмы (применено 2026-08-23):
Проверка ticker_settings.json показала, что целевые значения уже
применены:
- RENI: 4 (было 50)
- BSPB: 10 (было 50)
- LENT: 20 (было 50)
- CNRU: 9 (было 50)
Это должно помочь детектору видеть коротких роботов конкурента
(qty 10-50). Эффект проверим после сбора свежей ленты с v3.20.

Что не трогали пока (не успели):
- GUI-вкладки для новых детекторов (владелец отложил: "потом").
- Трёхногие связки (symbol_c) — v2.
- Свечи из Quik для графиков — v3.
- Индикативная цена аукциона из стакана.
- Telegram под absolute_rub.

Следующие шаги (после применения v3.20)
1. Понедельник: применить export_trades.lua v3.20, собрать свежую
   ленту за обычную сессию.
2. Снять эталон конкурента за тот же день.
3. Прогнать ab_compare.py на новых данных.
4. Если TP > 0: зафиксировать метод по флагам как основной.
5. Если TP = 0: откатиться на tick-rule (6-е поле), проверить
   следующую гипотезу — несовпадение по времени (наш timestamp vs
   время в competitor_supplement_*.jsonl). Проверить руками 2-3
   FN-случая: есть ли в quik_trades.csv сделки того же тикера/объёма
   в окне ±30 сек от времени в эталоне.
6. Параллельно: докрутка детектора (смягчение jitter, grid_lock,
   interval_tolerance) на основе анализа FP.
7. Вид главного окна — при решении владельца приблизить к конкуренту.
8. Вкладки GUI для новых детекторов (volume_spike, level_break, spring).

План на понедельник 2026-08-25 (получен от ревьюера 2026-08-23 15:14)
======================================================================

Критическое замечание: flags%2 и tick-rule дают 97.3% совпадений —
это почти идентичные методы. Если в понедельник TP=0 с flags%2, это
НЕ докажет проблему стороны. Один контрольный прогон, если TP=0 —
сразу к детектору. Не тратить время на сторону.

Шаги:

1. Контрольный прогон ab_compare с flags%2 как основным методом
   (v3.20 уже применена). Интерпретация: если TP не изменился —
   сразу к п.2, не ковырять сторону дальше.

2. Главная работа — детектор. Поднять short_interval_threshold
   с 10.0 до 20.0 в core/config.py.
   Основание: волны конкурента имеют интервалы 12-18с, они не
   попадают под адаптивный допуск 12% (short_interval_tolerance)
   и получают базовый жёсткий допуск 5% — режутся.

3. Прогнать ab_compare после п.2, сравнить с baseline
   (TP=0, FN=142, FP=1270). Зафиксировать в таблице ниже.

4. Если TP всё ещё 0 — смягчать по одному параметру за раз,
   с прогоном ab_compare после каждого:
   a) interval_tolerance: 0.05 → 0.08
   b) grid_tolerance_ms: 700 → 1000
   c) jitter_ratio_max: 0.3 → 0.5 (или временно 0 для проверки)

5. После каждого шага п.4 смотреть и FP (шумные ложные серии
   100-600с LEN 2-3). Если FP резко растёт — откатить параметр
   и пометить "не трогать дальше".

6. Расширение зондов (параллельно, не блокируя основную работу):
   - probe_datasource — минимум 15-20 активных тикеров за
     несколько дней (443 сделки в пн — слишком мало).
   - compare_sides — ещё 2-3 дня, кроме 08-21.
   - Если 87.6% подтвердится многодневно — вернуть tick-rule
     основным методом, flags%2 оставить только для диагностики
     (выигрыша от flags%2 нет).

7. min_qty (после сбора свежей ленты понедельника):
   - Проверить diagnose_fn по RENI/BSPB/LENT/CNRU — пропадают
     ли они из FN.
   - Если да — применить тот же приём к остальным тикерам.
   - Формализовать скриптом (расширить tune_all.py или отдельный).

Отложено:
- GUI-вкладки volume_spike/level_break/spring
- Трёхногие связки (symbol_c)
- Telegram под absolute_rub

Свип параметров детектора 2026-08-25 (выполнен по плану)
======================================================================
Эталон вырос до 730 записей (+349 за 2026-08-25 из 55 скринов,
10:07–14:00). Файл: research/competitor_supplement_2026-08-25.jsonl.

Результаты пошагового свипа (по одному параметру за раз, ab_compare):

- baseline (эталон 142, 2026-08-22): TP=0, FN=142, FP=1270
  (старый эталон, до supplement).
- после применения v3.20 (flags%2) + min_qty по тикерам +
  threshold=20.0 (план п.2) на новом эталоне 730:
  TP=49, FN=681, FP=36254. **Прорыв: TP>0**. Детектор видит
  роботов конкурента на ленте v3.20.
- grid_tolerance_ms: 700→1000 (план п.4b): TP=13 (урон), откат.
- jitter_ratio_max: 0.3→0.0 (план п.4c, временный эксперимент):
  TP=21 (рост), но FP=28047 (взрыв шума 8×). Откат по п.5 плана.
- финальная конфигурация (зафиксирована в core/config.py):
  threshold=20.0, grid_tolerance_ms=700, jitter_ratio_max=0.3.
  TP=49, FN=681, FP=36254.

Выводы по свипу:
- TP вырос с 0 до 49 (флаги + min_qty + threshold=20).
- FN ещё 681 — волны конкурента часто не проходят по интервалу
  (наш детектор видит СОВСЕМ другие серии по тем же тикерам,
  ratio интервалов 4-20× в diagnose_fn).
- FP огромный (36254) — наш поток шумит длинными сериями; это
  предмет дальнейшей работы, но трогать grid/jitter больше нельзя
  (они дают либо урон TP, либо взрыв FP).
- interval_tolerance=0.05 в базовом config.py перебивается
  per-ticker override в ticker_settings.json (у всех тикеров 0.05).
  Следующий одиночный эксперимент (план п.4a) — поднять до 0.08
  через ticker_settings.json отдельным прогоном.

Применённые правки (закоммичены):
- research/competitor_supplement_2026-08-25.jsonl (новый, 349 строк)
- tools/make_supplement_2026_08_25.py (новый)
- core/config.py: short_interval_threshold 10.0→20.0
- core/config.py: v11 — jitter_ratio_max возвращён к 0.3

Диагностика FN (ложно-отрицательные) по CNRU (2026-08-25, анализ 24 случаев)
======================================================================
Проблема: CNRU buy ref_int=10.0 ref_qty=[7,8] — 24 FN-случая в эталоне,
все пропущены детектором.

Анализ quik_trades.csv за 2026-08-25 (проверка руками):
- Сделки CNRU qty=7-8 **есть** в ленте (строки 33939, 69872, 73662, ...)
- Но это сделки за 08-18 (epoch ~1787063xxx), не за 08-25
- За 08-25 в окне 10:07-14:00 сделок CNRU qty=7-8 **нет** в CSV

Вывод: lua-скрипт export_trades.lua **не записал** сделки CNRU qty=7-8
за 2026-08-25 в CSV. Это объясняет, почему детектор их не видит — сделок
просто нет в ленте.

Гипотезы:
1. Lua-скрипт не работал в момент 10:07-14:00 (пропуск сделок).
2. Quik не вызвал OnAllTrade для CNRU в тот момент (редкая ситуация).
3. Сделки CNRU qty=7-8 есть в CSV, но в другое время (не 10:07-14:00).

Проверка: нужны все сделки CNRU за 08-25 (не только в окне 10:07-14:00).
Если сделок с qty=7-8 вообще нет за весь день — проблема в lua-скрипте.
Если есть, но в другое время — проблема во временной привязке эталона.

Следующий шаг: проверить все сделки CNRU за 08-25 через quik_csv_diag.py
или прямой grep по CSV.

Эксперимент interval_tolerance 0.05→0.08 (план п.4a)
======================================================================
Проведён 2026-08-25 через per-ticker override в ticker_settings.json.

Результат: TP=49, FN=681, FP=36254 — **без изменений** (цифры идентичны
финальной конфигурации свипа).

Корень проблемы: параметр `interval_tolerance` в базовом config.py
перебивается per-ticker override в ticker_settings.json (у всех тикеров
0.05). Но даже если бы override сработал — эффект был бы нулевой, потому
что волны конкурента (12-18с) попадают под `short_interval_tolerance=0.12`
(адаптивный допуск для интервалов <20с), а не под базовый `interval_tolerance`.

Вывод: эксперимент не дал эффекта. Откат 0.08→0.05 выполнен (77 тикеров
восстановлены).

Этап 1: метрика FP (ложно-положительные) (2026-08-25)
======================================================================
**Проблема (подтверждена ревью):** FP=36254 считал ВСЕ наши сигналы без
пары в эталоне за все дни. Это мусорная метрика — большинство сигналов
не противоречат эталону, а просто не попали в моменты скриншотов.

**Решение:** разделить несматченные сигналы на два класса:
- **FP реальные:** в окне жизни серии [start, end+600с] ЕСТЬ момент
  скриншота конкурента (конкурент смотрел на этот тикер в это время
  и не увидел наш робот / увидел другого).
- **UNVERIFIED:** скриншотов в окне нет, сравнивать не с чем (конкурент
  не делал скриншот в этот момент).

**Применённые правки (research/ab_compare.py):**
- Добавлены `build_shots_by_date`, `has_screenshot_in_window` (bisect
  по сортированному списку моментов скриншотов).
- Разбиение `fp_list` на реальные/UNVERIFIED.
- Формат кэша не изменён → существующий кэш переиспользуется.

**Результат (прогон ab_compare_2026-08-25_metric_fix.txt):**
- TP=49 (не изменился, матчинг не тронут)
- FN=681 (не изменился)
- FP реальные=19388 (скриншот в окне)
- UNVERIFIED=16866 (скриншота нет)
- Сумма: 19388+16866=36254 (сходится со старым FP)

**Вывод:** метрика теперь осмысленная. 19388 — серии, которые
конкурент в момент скриншота НЕ видел (настоящие ложные срабатывания).
16866 — серии вне моментов скриншотов (не с чем сравнивать).

Этап 2: диагностика CNRU через check_window_ticker (2026-08-25)
======================================================================
Первичная гипотеза (отвергнута): lua-скрипт не писал сделки CNRU.
Гипотеза "строгого чередования buy/sell" (отвергнута): стороны НЕ
чередуются регулярно.

**Факты из check_window_ticker.py CNRU 2026-08-25 10:10 10:20**
(338 сделок в окне):
- Робот конкурента **присутствует** в нашей ленте: buy каждые ~10с,
  qty 4-8, с 10:10:12 до 10:19:52 (10+ ударов подряд).
- Робот тонет в двух видах шума:
  1. Мелкие buy qty 1-3 вперемешку (не проходят min_qty=9).
  2. Периодические burst-sell: пачки по 20-60 сделок с gap=0,
     qty до 800 (10:11:18, 10:12:37, 10:14:14, 10:17:45).

**Обновлённая гипотеза корня проблемы:**
Интервалы серии buy НЕ строго 10с. Пример из лога:
- 10:10:12 qty=8.0 buy gap=10.0s
- 10:10:22 qty=7.0 buy gap=1.0s (двойной удар через 1с!)
- 10:10:32 qty=7.0 buy gap=10.0s
- 10:10:42 qty=4.0 buy gap=10.0s

Если отфильтровать qty=7 с gap=1с (двойной удар, не новый), интервал
становится ~20с, а не 10с. Детектор видит серию с interval_avg ~20с,
а эталон — 10с. Матчинг в ab_compare.py проваливает по ratio
(20/10 = 2.0, не в [0.7, 1.3]).

**Вывод:** проблема НЕ в чередовании сторон, а в "двойных ударах"
(две сделки через 1с с одним qty). Детектор считает их двумя ударами,
интервал ломается. Нужен фильтр двойных ударов в детекторе (интервал
< 2с = тот же удар, не новый) или изменение матчинга в ab_compare
(ratio до 2.0 для кратных интервалов).

Следующий шаг: узкое окно 10:12-10:14 для подтверждения гипотезы.

Этап 3: фильтр двойных ударов (2026-08-25, реализовано)
======================================================================
**Применено:** детектор v10 в detectors/interval_robot.py.

**Правка:**
- Новый параметр `min_double_hit_gap_sec` (default 2.0) — порог
  фильтра двойных ударов (burst-sell, шум с gap<2с).
- В методе `on_trade`, после вычисления `iv = ts - match.last_ts`,
  проверяется: если `iv < min_double_hit_gap_sec`, то это шум
  (тройной удар или burst-sell), серия НЕ обновляется.
- Логирование `DOUBLE_HIT_SKIP` для отладки.
- Коммит b44b7d3.

**Ожидаемый эффект на CNRU:**
- Тройной удар в 10:12:02 (qty 3, 2, 2 с gap=0) больше не ломает
  базу серии.
- Детектор должен увидеть чистый робот: buy каждые 10с, qty 3-8.

**Проверка:** прогон ab_compare.py --recalc после коммита.
Ожидаем рост TP (CNRU переходит из FN в TP) без роста FP.

Финальные выводы по этапам 1-2 (2026-08-25)
======================================================================
**Этап 1 (метрика FP) — завершён:**
- Исправлен research/ab_compare.py: FP теперь осмысленная метрика.
- Результат: TP=49, FN=681, FP=19388, UNVERIFIED=16866.
- Коммит 9e18870.

**Этап 2 (диагностика CNRU) — завершён:**
- Узкое окно CNRU 10:12-10:14 (check_window_ticker.py, 156 сделок):
  - **Робот есть** (первые 7 строк: buy каждые 10с, qty 3-8).
  - **Тройной удар** в 10:12:02 (qty 3, 2, 2 с gap=0) ломает базу.
  - **Burst-sell** в 10:12:37 (80+ сделок за секунду, qty до 800).
  - **Шум после burst** (10:12:38-10:13:52): интервалы 1s, 2s, 3s, 4s,
    6s, 8s — НЕ 10с (не робот, а реакция на большой ордер).
  - **Финал** (10:13:32-10:13:52): интервалы 6s, 2s, 8s, 10s — НЕ 10с.

**Корень проблемы CNRU (подтверждён):**
- Детектор не видит робота из-за:
  1. Тройного удара в начале серии (gap=0, qty разные).
  2. Burst-sell + последующий шум с интервалами ≠10с.
- Фильтр "двойных ударов" (интервал < 2с = тот же удар, не новый)
  **поможет** убрать шум от тройного удара.

**Следующий шаг (этап 3):**
- Правка детектора interval_robot.py: добавить фильтр двойных ударов
  в метод `on_trade` или `_find_match`.
- Логика: если interval < min_double_hit_gap (например, 2.0с),
  считать это тем же ударом, не создавать новую серию и не обновлять
  интервал существующей.
- Тестирование: прогон ab_compare после правки, сравнение TP/FN/FP.

Таблица прогонов ab_compare:
+------------+-------------------------------+------+------+------+------------------------+
| Дата       | Правка                        | TP   | FN   | FP   | Примеч.                |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-22 | baseline                      | 0    | 142  | 1270 | эталон=142             |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | v3.20+min_qty+threshold=20    | 49   | 681  | 36254| эталон=730, baseline   |
|            |                               |      |      |      | свипа                  |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | grid_tolerance_ms=1000        | 13   | 717  | 3295 | ОТКАТ (урон TP)        |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | jitter_ratio_max=0.0          | 21   | 709  | 28047| ОТКАТ (FP взрыв 8×)    |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | финал: thr=20+grid=700+jit=0.3| 49   | 681  | 36254| зафиксировано          |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | interval_tolerance=0.08       | 49   | 681  | 36254| ОТКАТ (эффекта нет)    |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | метрика FP (реальные/UNVERIF) | 49   | 681  | 19388| FP=19388, UNVERIF=16866|
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-25 | детектор v10 (double-hit)     |      |      |      | ЖДЁМ РЕЗУЛЬТАТ         |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-26 | v10.1 (double-hit=1.0)        | 21   | 709  | 2092 | урон TP (было 49)      |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-26 | CNRU min_qty=5, ratio=1.3     | 25   | 705  | 2092 | +4 TP (все CNRU)       |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-26 | RENI/LENT/BSPB ratio=1.3      | 25   | 705  | 2092 | эффекта нет            |
+------------+-------------------------------+------+------+------+------------------------+
| 2026-08-26 | новая разбивка FP             | 44   | 686  | 4422 | спорные=4150,          |
|            | (спорные/без_стороны/нет_дан) |      |      |      | без_стороны=44,        |
|            |                               |      |      |      | нет_данных=228         |
+------------+-------------------------------+------+------+------+------------------------+
|            |                               |      |      |      |                        |
+------------+-------------------------------+------+------+------+------------------------+

Достижения 2026-08-26
======================================================================
**Прогресс-бар и лог-файл (research/ab_compare.py):**
- Прогресс-бар v2: авто-определение терминал/файл (через `isatty()`).
  В терминале обновляется на месте (`\r`), в файле — каждая строка
  на новой линии с временной меткой [HH:MM:SS].
- Лог-файл UTF-8: скрипт сам пишет в `output/ab_compare_log_<время>.txt`
  через класс `_TeeStream`, минуя перекодировку PowerShell (которая
  ломает символы █░ при перенаправлении `>`).
- Прогресс-бар теперь только ASCII-символы (`#` и `.`) — не ломается
  ни в какой кодировке.

**Новая метрика FP (разбивка по достоверности):**
- **Спорные** — тикер+сторона ЕСТЬ в эталоне (конкурент смотрел на этот
  тикер, но именно эта серия не совпала). Это единственная категория,
  которую стоит разбирать как потенциальный настоящий ложный сигнал.
- **Тикер без стороны** — тикер в эталоне есть, но с другой стороной.
- **Нет данных** — тикера вообще нет в эталоне ни разу за эти даты,
  сравнивать не с чем, в счёт "ошибок" не идёт.

**Результат прогона с новой разбивкой (2026-08-26, эталон=730):**
- TP=44 (+19 к предыдущему 25)
- FN=686
- FP всего=4422 (из них **спорные=4150**, без_стороны=44, нет_данных=228)

**CNRU — подтверждение корня проблемы:**
- **Гипотеза "чередование сторон" опровергнута** (check_window_ticker.py
  показал: стороны НЕ чередуются регулярно, робот устойчиво односторонний).
- **Корень проблемы — тривиальный баг порогов:**
  - `min_qty=9` резал сделки qty=7-8 (робот физически не мог накопить
    ни одного повтора).
  - `max_qty_ratio=1.10` не объединял qty=7 и qty=8 в одну серию
    (ratio 8/7=1.143 > 1.10).
- **Применено:** CNRU min_qty 9→5, ratio 1.10→1.3.
- **Результат:** CNRU появился в TP (4 записи `buy [7,8] int=10.0`).
- **Sideless-режим НЕ нужен** — проблема была в порогах, не в архитектуре.

**Новый инструмент (tools/check_min_qty_vs_ref.py):**
- Массовый поиск тикеров, где `min_qty` в `ticker_settings.json` выше
  реального объёма робота конкурента.
- Результат: найдено 39 тикеров с разрывом (топ: FIXR 50→1, ASTR 50→5,
  PLZL 50→6, MTSS 50→6, MSNG 50→6).
- **ВАЖНО:** скрипт только читает, ничего не меняет. Применять правки
  в `ticker_settings.json` — отдельным шагом, после ручной проверки
  3-5 строк на скринах (как сделано для CNRU).

**ticker_settings.json возвращён в репозиторий:**
- Файл был в `.gitignore` (строка 2), все правки `min_qty`/`max_qty_ratio`
  жили только локально и терялись при `git checkout`.
- Убрано из `.gitignore`, добавлено принудительно (`git add -f`).
- Коммит b2ae39b: 2 files changed, 851 insertions (полный файл настроек).
- Теперь настройки тикеров под версионным контролем.

**Подтверждённые тикеры (появились в TP 2026-08-26):**
- ✅ **CNRU**: min_qty 9→5, ratio 1.10→1.3
- ✅ **RENI**: min_qty=4, ratio=1.3 (несколько записей в TP)
- ✅ **MAGN**: sell [41,42], [52,53] (несколько записей)
- ✅ **BELU**: sell [16,17], [29,30]
- ✅ **ROSN**: sell [130,139]
- ✅ **SBER**: buy/sell (несколько записей)

**Топ-5 спорных FP (для ручного разбора):**
1. RENI buy qty=4-5 повт=36 int=15.0 (наш детектор нашёл, эталон не видел)
2. MVID sell qty=125-126 повт=29 int=16.0
3. ROSN sell qty=62-65 повт=24 int=16.0
4. SNGSP sell qty=26-27 повт=21 int=11.0
5. HEAD sell qty=20-20 повт=21 int=205.5 (jitter=50208мс — подозрительно)

Следующие шаги (2026-08-26, после фиксации достижений)
======================================================================
**Вариант A: Разбор спорных FP** (рекомендую)
- Выбрать 1-2 тикера из топ спорных (например, RENI buy qty=4-5 повт=36
  или HEAD sell qty=20-20 с огромным jitter).
- Проверить руками: есть ли скриншоты конкурента в эти моменты?
- Если эталон просто не заскринил — это не ошибка детектора.
- Если детектор действительно ложно сработал — искать корень.

**Вариант B: Эксперимент с фильтром v10**
- Отключить `min_double_hit_gap_sec` (сейчас 1.0с) на всех тикерах.
- Прогнать ab_compare, сравнить TP.
- Ожидаем рост TP до ~49 (как было до v10) без взрыва FP.
- Если подтвердится — фильтр v10 работает против нас, отключаем насовсем.

**Вариант C: Продолжение точечных настроек**
- По данным `check_min_qty_vs_ref.py` ещё 39 тикеров с разрывом min_qty.
- Проверить руками FIXR/MSNG/PLZL (топ разрыва).
- Если подтверждаются — точечно править `ticker_settings.json`.

**Вариант D: Сбор данных за 2026-08-26**
- Lua-скрипт писал ленту весь день.
- Нужен эталон конкурента за тот же день (скриншоты).
- Если есть — импортировать через `tools/import_competitor_csv.py`,
  прогнать ab_compare на свежих данных.

Правила документации:
- Полный вывод ab_compare сохранять в output/ab_compare_<дата>.txt
- Таблица — построчно, не сплошным текстом
- Каждый шаг фиксировать с TP/FN/FP

Находки 2026-09-04 (критично)
======================================================================
**Стороны QUIK правильные (tick-rule НЕ проблема):**
- Сверка с Алором (compare_sides.py): 99.2% совпадений за 2026-09-03
- Корень FN — высокие min_qty режут мелких роботов

**A/B --no-min-qty (потолок Recall):**
- TP 14→71, но FP 8325→25812 (взрыв шума qty=[1,1] повт=4)
- Вывод: min_qty=1 неприемлем, нужны точечные пороги

**Диагностика FN (check_fn_series.py):**
- X5 sell 60.1с: сетка ЕСТЬ (удары в :20), но ломается случайными
  сделками того же объёма — класс «загрязнённая сетка»
- OZON/LKOH: стабильной сетки в ленте НЕТ — T-Widgets агрегирует
  burst-ы; это их эвристика, не воспроизводим

**Точечные min_qty (коммит d290d63):**
LKOH 50→2, PLZL 50→9, MSNG 50→2, MTSS 50→6, OZON 45→5

**Результат (replay_alor за 2026-09-03):**
- TP 14→86 (+514%), FP 8325→13566 (+63%), FN 764→692
- Recall 1.8%→11.1%, Precision 0.2%→0.6%
- Вывод: точечные пороги работают без взрыва FP

**Новые инструменты (коммиты 60da289, a96f45a):**
- tools/alor_download_day.py — alltrades Алора (data/<TICKER>_ДАТА.json)
- research/replay_alor.py — реплей по истинным сторонам
  (--no-min-qty, --double-hit, изолированная история)
- research/compare_sides.py, research/check_fn_series.py
- Починка Алора: oauth.alor.ru/refresh (POST), to < сегодня

Следующие шаги (2026-09-05)
======================================================================
1. Скачать Алор за 2026-09-04 (API откроется после 03:00 МСК)
2. replay_alor + tw_compare за 2026-09-04, сравнить с 09-03
3. Живой прогон приблуды весь день, вечером сверка
4. Recall < 15% — продолжать точечные min_qty по tw_fn_summary
5. Recall > 20% — правка детектора под «загрязнённую сетку»
   (grid-lock с первого удара): сначала тесты, потом коммит

Отложено:
- GUI-вкладки volume_spike/level_break/spring
- Трёхногие связки (symbol_c), Telegram под absolute_rub
- Агрегация burst-ов (класс «загрязнённая сетка»)